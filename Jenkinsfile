pipeline {
    agent any
    stages {
        stage('run') {
            steps {
                echo 'Clarusway_Way to Reinvent MYselfbb'
                sh 'python --version'
                sh 'python pipeline.py'
            }
        }
    }
}
